{
  "openapi": "3.1.0",
  "info": {
    "title": "Demo User Service API",
    "description": "Sanitized fixture for OpenAPI 3.1 normalization tests (no real hosts).",
    "version": "v1"
  },
  "servers": [
    {
      "url": "http://localhost:9002",
      "description": "Placeholder server URL"
    }
  ],
  "paths": {
    "/users/{id}": {
      "get": {
        "operationId": "getById",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": { "type": "string" }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/User"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "User": {
        "type": "object",
        "properties": {
          "id": { "type": "string" },
          "email": { "type": "string" }
        }
      }
    }
  }
}
